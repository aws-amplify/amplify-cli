import { resolve, join } from 'path';

export const unifiedChangelogPath = resolve(join(__dirname, '..', 'UNIFIED_CHANGELOG.md'));

export const eslintDictionaryPath = resolve(join(__dirname, '..', '.eslint-dictionary.json'));
