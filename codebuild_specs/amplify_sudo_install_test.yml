version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source ./shared-scripts.sh && _amplifySudoInstallTestSetup
      - sudo npm install -g @aws-amplify/cli
      - source .circleci/local_publish_helpers.sh && unsetSudoNpmRegistryUrl
      - amplify version
