version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source ./shared-scripts.sh && test
      - source ./shared-scripts.sh && _buildLinux
  post_build:
    commands:
      - aws s3 ls s3://$CACHE_BUCKET_NAME --recursive
