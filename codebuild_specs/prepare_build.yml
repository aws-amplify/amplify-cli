---
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - yarn config set script-shell $(which bash)
      - yarn
  pre_build:
    commands:
      - echo running prebuild
      - yarn config -g set prefer-offline true
      - yarn config -g set cache /root/.yarn
      - yarn config get cache
  build:
    commands:
      # - yarn production-build
  post_build:
    commands:
      - ls
      - ls ~/.cache
      - ls ~/.ssh
      - ls ~/.
      - ls ~
      - ls .
      - ls /root
cache:
  paths:
    - ~/.cache/yarn/**/*
