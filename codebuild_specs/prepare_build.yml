---
version: 0.2
artifacts:
  files:
    - '**/*'
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - yarn install
  pre_build:
  build:
    commands:
      - yarn run production-build
      - yarn build-tests
  post_build:
    commands:
      -
cache:
  paths:
    - ~/.cache/yarn/**/*
