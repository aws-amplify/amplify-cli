version: 0.2
phases:
  pre_build:
    commands:
      - source shared-scripts.sh
  build:
    commands:
      - _buildLinux
  post_build:
    commands:
      - aws s3 ls s3://$ARTIFACT_BUCKET_NAME --recursive
