version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo "Build Tests Standalone"
      - source ./shared-scripts.sh && _buildTestsStandalone
  post_build:
    commands:
      - echo $PROJECT_NAME
      - echo $BRANCH_NAME
      - echo "BRANCH_NAME=$BRANCH_NAME CODEBUILD_WEBHOOK_TRIGGER=$CODEBUILD_WEBHOOK_TRIGGER CODEBUILD_WEBHOOK_TRIGGER=$CODEBUILD_WEBHOOK_TRIGGER"
artifacts:
  files:
    - "shared-scripts.sh"
