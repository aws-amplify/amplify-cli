version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo "Build Tests Standalone"
      - source ./shared-scripts.sh && _buildTestsStandalone
artifacts:
  files:
    - 'shared-scripts.sh'
