version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo integration testing
      - export CODEBUILD_SOURCE_VERSION=e8a74b9d09bf6653efe0e8c9bbb46c79cc65b4be #this injects an older workflow version into shared-scripts
      - source ./shared-scripts.sh && _integrationTest
