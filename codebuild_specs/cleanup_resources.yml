version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - cd ./scripts
      - npm install -g ts-node
      - npm install aws-sdk
      - cd ..
      - source ./shared-scripts.sh && _waitForJobs aggregate_e2e_reports
      - source ./shared-scripts.sh && _cleanUpResources
