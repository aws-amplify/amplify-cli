version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo CODECOV
      - echo $CODECOV_TOKEN
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
