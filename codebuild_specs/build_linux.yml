version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - ./.circleci/codebuild-checkout.sh
      - codebuild-breakpoint
      - source ./shared-scripts.sh && _buildLinux
      - codebuild-breakpoint

artifacts:
  files:
    - 'shared-scripts.sh'
