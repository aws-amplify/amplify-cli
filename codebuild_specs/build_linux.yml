version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - su codebuild-user
      - whoami
      - su root
      - whoami
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
