version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo "${CODEBUILD_WEBHOOK_TRIGGER#branch/*}"
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
