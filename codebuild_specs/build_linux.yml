version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo "${CODEBUILD_SOURCE_VERSION#branch/*}"
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
