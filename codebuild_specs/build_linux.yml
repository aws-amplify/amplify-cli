version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - node -v
      - npm -v
      - yarn -v
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
