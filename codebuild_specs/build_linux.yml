version: 0.2
env:
  shell: bash
phases:
  build:
    run-as: codebuild-user
    commands:
      - whoami
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
