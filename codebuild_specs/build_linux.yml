version: 0.2
env:
  shell: bash
  compute-type: BUILD_GENERAL1_LARGE
phases:
  build:
    commands:
      - ./.circleci/codebuild-checkout.sh
      - source ./shared-scripts.sh && _buildLinux

artifacts:
  files:
    - 'shared-scripts.sh'
