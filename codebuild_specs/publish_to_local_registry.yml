version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source ./shared-scripts.sh && source ./.circleci/local_publish_helpers.sh && _publishToLocalRegistry

artifacts:
  files:
    - 'shared-scripts.sh'
