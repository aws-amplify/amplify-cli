version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source shared-scripts.sh && _waitForJobs build_linux
      - source ./shared-scripts.sh && _publishToLocalRegistry
      - exit 1

artifacts:
  files:
    - 'shared-scripts.sh'
