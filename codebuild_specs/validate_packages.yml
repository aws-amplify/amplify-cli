version: 0.2
cache:
  paths:
    - /root/yarn/**/*
    - build/**/*
phases:
  post_build:
    commands:
      - yarn lint-check
      - yarn test-ci
      - yarn verify-api-extract
      - yarn verify-yarn-lock
      - echo Post build complete
