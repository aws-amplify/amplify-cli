version: 0.2
cache:
  paths:
    - ~/.cache/yarn
phases:
  post_build:
    commands:
      - ls
      - cd ~/.cache/yarn
      - yarn lint-check
      - yarn test-ci
      - yarn verify-api-extract
      - yarn verify-yarn-lock
      - echo Post build complete
