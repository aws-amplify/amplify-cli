version: 0.2
cache:
  paths:
    - /root/.npm/**/*

phases:
  pre_build:
    commands:
      - ls ~/.
      - ls ~
      - ls .
      - ls /root
      - ls ~/.cache
  post_build:
    commands:
      - npm lint-check
      - cd packages/amplify-util-mock && yarn e2e
      - npm test-ci
      - npm verify-api-extract
      - npm verify-yarn-lock
      - echo Post build complete
