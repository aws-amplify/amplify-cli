version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      # - node -v
      # - npm -v
      # - nvm version
      # - nvm install 18.15.0
      # - nvm use 18.15.0
      # - nvm ls
      # - node --version
      # - npm --version
      - npm install -g yarn
      - npm install -g node-gyp
      - yarn run production-build
      - yarn build-tests

artifacts:
  files:
    - 'shared-scripts.sh'
