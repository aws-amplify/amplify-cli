version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node --version
      - npm --version
      - bash
      - source ./shared-scripts && _buildWindows
      - sh source ./shared-scripts && _buildWindows
      - bash
      - source ./shared-scripts && _buildWindows
      - yarn run production-build
      - yarn build-tests

artifacts:
  files:
    - 'shared-scripts.sh'
