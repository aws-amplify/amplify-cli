version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node --version
      - npm --version
      - yarn -v
      - yarn run production-build
      - yarn build-tests
      - bash ./save-build-windows.sh

artifacts:
  files:
    - 'shared-scripts.sh'
