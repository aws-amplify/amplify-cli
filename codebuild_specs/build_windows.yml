version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node -v
      - npm -v
      - n install 18
      - yarn run production-build
      - yarn build-tests

artifacts:
  files:
    - 'shared-scripts.sh'
