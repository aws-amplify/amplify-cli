version: 0.2
env:
  shell: powershell.exe
  imagePullCredentialsType: CODEBUILD
phases:
  build:
    commands:
      - source ./shared-scripts.sh && _buildWindows

artifacts:
  files:
    - 'shared-scripts.sh'
