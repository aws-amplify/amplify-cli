version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node --version
      - npm --version
      - npm install -g yarn
      - bash ./build-windows.sh

artifacts:
  files:
    - 'shared-scripts.sh'
