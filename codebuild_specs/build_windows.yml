version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - yarn --immutable
      - yarn production-build
      - yarn build-tests
      - bash ./codebuild_specs/scripts-windows/save-build-windows.sh

artifacts:
  files:
    - 'shared-scripts.sh'
