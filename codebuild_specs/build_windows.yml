version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node --version
      - npm --version
      - (bash source ./shared-scripts-windows.sh) -and (bash _buildWindows)

artifacts:
  files:
    - 'shared-scripts.sh'
