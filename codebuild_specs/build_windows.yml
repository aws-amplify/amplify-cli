version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - node --version
      - npm --version
      - sh ./shared-scripts.sh && _buildWindows
      - yarn run production-build
      - yarn build-tests

artifacts:
  files:
    - 'shared-scripts.sh'
