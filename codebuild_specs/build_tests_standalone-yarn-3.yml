version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo "Build Tests Standalone"
      - source ./shared-scripts.sh && _buildTestsStandaloneYarn3
artifacts:
  files:
    - 'shared-scripts.sh'
