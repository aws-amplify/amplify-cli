type Record
  @model
  @key(name: "byNameCreatedAt", fields: ["name", "createdAt"])
  @key(name: "byNameUpdatedAt", fields: ["name", "updatedAt"]) {
  id: ID!
  name: String
  owner: String
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}
