describe('fake test remember to delete', () => {
  const x = 1;
  const y = 1;
  const z = 2;

  it('will pass 3', async () => {
    expect(x).toEqual(y);
  });

  it('will pass 4', async () => {
    expect(x).toEqual(x);
  });
});
