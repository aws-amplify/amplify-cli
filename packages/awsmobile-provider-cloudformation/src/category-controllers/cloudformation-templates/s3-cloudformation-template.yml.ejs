Resources:
  S3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      AccessControl: <%= props.policy %>
      BucketName: <%= props.bucketName %>
Outputs:
  BucketName:
    Value: <%= props.bucketName %>
    Description: Bucket name for the S3 bucket