const addProviderAction = require('./add-provider'); 
const removeProviderAction = require('./remove-provider'); 
const switchFrontendHandlerAction = require('./switch-frontend-handler'); 

function configure(context){
}

module.exports = {
    configure
}