import { StorageDynamoRollbackRefactorer } from '../../../../../commands/gen2-migration/refactor/storage/storage-dynamo-rollback';

describe('StorageDynamoRollbackRefactorer', () => {
  it('is defined', () => {
    expect(StorageDynamoRollbackRefactorer).toBeDefined();
  });
});
