import { StorageS3RollbackRefactorer } from '../../../../../commands/gen2-migration/refactor/storage/storage-rollback';

describe('StorageS3RollbackRefactorer', () => {
  it('is defined', () => {
    expect(StorageS3RollbackRefactorer).toBeDefined();
  });
});
