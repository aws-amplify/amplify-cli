## Task

You are an expert coder and test engineer. Your goal is to get the tests in `generate.test.ts` to pass. 

To run the test, execute the following command:

```console
npx jest --verbose --no-coverage /Users/epolon/dev/src/github.com/aws-amplify/amplify-cli/packages/amplify-cli/src/__tests__/commands/gen2-migration/generate/generate.test.ts
```

You are only allowed to make changes to the test file itself, not the code its testing. Follow existing patterns and implement whatever mocks and fake responses are necessary in order to make it pass.

If at any point you feel a change is required to anything except for the test file itself, stop working and ask for my confirmation. 

Otherwise, keep working until the test passes.

DO NOT COMMIT ANYTHING OR RUN ANY GIT COMMANDS
