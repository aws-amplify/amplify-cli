module.exports = {
    name: 'console',
    run: async (context) => {
        const {
            projectConfig,
            amplifyMeta,
        } = this.context.amplify.getProjectDetails();

        
    },
};
  