<%= props.topLevelComment %>

exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
    <%- props.uncommentCorsHeader ? 
    `   headers: {
            \"Access-Control-Allow-Origin\": \"*\"
        },`
    :
    `//  Uncomment below to enable CORS requests
    //  headers: {
    //      "Access-Control-Allow-Origin": "*"
    //  },`
     %> 
        body: JSON.stringify('Hello from Lambda!'),
    };
    return response;
};
