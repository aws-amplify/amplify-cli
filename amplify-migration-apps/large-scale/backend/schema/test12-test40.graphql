# Models Test12 through Test40

type Test12 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test13 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test14 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test15 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test16 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test17 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test18 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test19 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test20 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test21 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test22 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test23 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test24 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test25 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test26 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test27 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test28 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test29 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test30 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test31 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test32 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test33 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test34 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test35 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test36 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test37 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test38 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test39 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}

type Test40 @model @auth(rules: [{ allow: public, operations: [read] }, { allow: owner }]) {
  id: ID!
  title: String!
  description: String
  status: ProjectStatus
  score: Int
}
