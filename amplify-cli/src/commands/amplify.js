module.exports = {
  name: 'amplify',
  run: async (context) => {
    const { print } = context

    print.info("Welcome to Amplify CLI. Here's the list of all the CLI commands");
    // List of all commands with brief descriptions
  }
}
