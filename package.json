{
  "name": "amplify-cli-v2",
  "version": "2.0.0",
  "description": "amplify-cli",
  "scripts": {
    "lint": "lerna run -- lint",
    "lint-fix": "lerna run -- lint-fix",
    "setup-dev": "lerna bootstrap && cd packages/amplify-cli && npm link"
  },
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
