{
  "name": "amplify-cli",
  "version": "0.1.0",
  "description": "amplify-cli",
  "scripts": {
    "lint": "lerna run -- lint",
    "lint-fix": "lerna run -- lint-fix",
    "setup-dev": "npx lerna@2.11 bootstrap && cd packages/amplify-cli && rm -f -- package-lock.json && npm link",
    "setup-dev-win": "npx lerna@2.11 bootstrap && cd packages/amplify-cli && del /f package-lock.json && npm link"
  },
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
